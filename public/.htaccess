<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Handle Angular/React Router (if needed)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>

